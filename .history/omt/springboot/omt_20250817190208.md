# Questions

## Q01

クラス間の依存関係を外部から注入する役割を担っているコンテナを〇〇コンテナという。

## Q02

SpringMVCのメリットとして不適切なものを選択せよ

UIにビジネスロジックを組み込むことで可読性が向上する

## Q03

コントローラーに下記記述が定義されている場合routerメソッドにマッチするURLを記入せよ

@Controller
@RequestMapping("/demo")
  public class DemoController{
   @RequestMapping("value=/index,method={
　　RequestMethod.GET,RequestMethod.POST}")
    　public String index(){
    }
}

<http://localhost:8080/demo/index>

## Q04

コントローラに下記の記述がされていて、リクエストURLが「<http://localhost:8080/demo/param/2021>」でリクエストされた場合、ブラウザで表示される適切なもの

@Controller
@RequestMapping("/demo")
public class DemoController{
   @GetMapping("/param/{id}")
   @ResponseBody
   public String param(@PathVariable Strind id){
    return "id:" + id;
    }
}

## Q05

コントローラに下記が実装されている　helloメソッドにマッチしたリクエストがあった場合の挙動として適切なものは

@Controller
@RequestMapping("/demo")
   public class DemoController{
   @GetMapping("hello"){
      return "/demo/hello";
  }
}

templatesフォルダ配下の「demo/helo.html」がレスポンスされる

## Q06

ントローラに下記が実装されている　variableメソッドにマッチしたリクエストがあった場合ビュー側でThymeleafを使用してブラウザに「こんにちは太郎さん」と出力するには

@GetMapping("variable")
   public String variable(Model model){
         model.addAttribute("name","太郎");
  return "demo/variable";
}

## Q07

モデルをエンティティとして定義する場合の記述として不適切なものを選択

A:テーブルのカラム（列）名の設定の「@Column」アノテーションの指定は必須である

## Q08

あるエンティティのリポジトリが定義されている場合、利用できるクエリメソッドとして不適切なものを選択

@Repository
public interface EmployeeRepository extends JpaRepository<Employee, Lonf>{
}

A:findByName()

## Q09

コントローラに社員情報を一覧するため実装されている。ビュー側でThymeleafを使用して記述するもの

@GetMapping("/employee_list")
public String showList(Model model){
  List<Employee> employees = repository.findAll();
  model.addAttribute("employees",employees);
  return "employee/list";
}

A:
<tr th:each="employee:${employees}"
      th:object="${employee}">
      <td th:text="*{id}"></td>
</tr>

## Q10

エンティティ「department」とエンティティ「employee」のリレーションが一対多であるときに適切な記述

A:public class Department{
        :
}
public class Employee{
       @ManyToOne
       private Department department;
              :
  
}

## Q11

モデルに検証ルールのアノテーション宣言を行います。
検証ルールでエラーとなる入力値はどれか

@Size(min = 1, max = 4)
private String name;

A:taro2

## Q12

コントローラのHandlerメソッドにモデル宣言した検証処理を有効にする記述する。
「キーワード」に入る適切なものを選択

public 型  Handlerメソッド（「キーワード」
@ModelAttribute 型　変数,BindingResult result,Model model){
  if (result.hasErrors()){
       return "入力フォームのパス";
     }
    :
}

A:@Validate

## Q13

モデルに「name」フィールドが定義されていてそのフィールドに対応するテンプレートが下記のように実装されている。
画面の「name」フィールドが未入力の場合検証エラーとして適切なものを選択
モデル
@Size(min = 1,max = 10)
private String name;

テンプレート
<input type="text" th:errorclass="スタイル"
th:field="*{フィールド}">
<div class="invalid-feedback" th:errors="*{フィールド}"></div>

A:1から10の間のサイズにしてください

## Q14

## Q15

## Q16

## Q17

## Q18

## Q19

## Q20
