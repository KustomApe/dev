# Questions

## Q01

クラス間の依存関係を外部から注入する役割を担っているコンテナを〇〇コンテナという。

## Q02

SpringMVCのメリットとして不適切なものを選択せよ

UIにビジネスロジックを組み込むことで可読性が向上する

## Q03

コントローラーに下記記述が定義されている場合routerメソッドにマッチするURLを記入せよ

@Controller
@RequestMapping("/demo")
  public class DemoController{
   @RequestMapping("value=/index,method={
　　RequestMethod.GET,RequestMethod.POST}")
    　public String index(){
    }
}

<http://localhost:8080/demo/index>

## Q04

コントローラに下記の記述がされていて、リクエストURLが「<http://localhost:8080/demo/param/2021>」でリクエストされた場合、ブラウザで表示される適切なもの

@Controller
@RequestMapping("/demo")
public class DemoController{
   @GetMapping("/param/{id}")
   @ResponseBody
   public String param(@PathVariable Strind id){
    return "id:" + id;
    }
}

## Q05

コントローラに下記が実装されている　helloメソッドにマッチしたリクエストがあった場合の挙動として適切なものは

@Controller
@RequestMapping("/demo")
   public class DemoController{
   @GetMapping("hello"){
      return "/demo/hello";
  }
}

templatesフォルダ配下の「demo/helo.html」がレスポンスされる

## Q06

ントローラに下記が実装されている　variableメソッドにマッチしたリクエストがあった場合ビュー側でThymeleafを使用してブラウザに「こんにちは太郎さん」と出力するには

@GetMapping("variable")
   public String variable(Model model){
         model.addAttribute("name","太郎");
  return "demo/variable";
}

## Q07

モデルをエンティティとして定義する場合の記述として不適切なものを選択

A:テーブルのカラム（列）名の設定の「@Column」アノテーションの指定は必須である

## Q08

あるエンティティのリポジトリが定義されている場合、利用できるクエリメソッドとして不適切なものを選択

@Repository
public interface EmployeeRepository extends JpaRepository<Employee, Lonf>{
}

A:findByName()

## Q09

コントローラに社員情報を一覧するため実装されている。ビュー側でThymeleafを使用して記述するもの

@GetMapping("/employee_list")
public String showList(Model model){
  List<Employee> employees = repository.findAll();
  model.addAttribute("employees",employees);
  return "employee/list";
}

A:

```html
<tr th:each="employee:${employees}"
      th:object="${employee}">
      <td th:text="*{id}"></td>
</tr>
```

## Q10

エンティティ「department」とエンティティ「employee」のリレーションが一対多であるときに適切な記述

A:public class Department{
        :
}
public class Employee{
       @ManyToOne
       private Department department;
              :
}

## Q11

モデルに検証ルールのアノテーション宣言を行います。
検証ルールでエラーとなる入力値はどれか

@Size(min = 1, max = 4)
private String name;

A:taro2

## Q12

コントローラのHandlerメソッドにモデル宣言した検証処理を有効にする記述する。
「キーワード」に入る適切なものを選択

```java
public 型  Handlerメソッド（「キーワード」
@ModelAttribute 型　変数,BindingResult result,Model model){
  if (result.hasErrors()){
       return "入力フォームのパス";
     }
    :
}
```

A:@Validate

## Q13

モデルに「name」フィールドが定義されていてそのフィールドに対応するテンプレートが下記のように実装されている。
画面の「name」フィールドが未入力の場合検証エラーとして適切なものを選択
モデル
@Size(min = 1,max = 10)
private String name;

テンプレート

```html
<input type="text" th:errorclass="スタイル"
th:field="*{フィールド}">
<div class="invalid-feedback" th:errors="*{フィールド}"></div>
```

A:1から10の間のサイズにしてください

## Q14

共通レイアウト「layout.html」が実装されていて、ヘッダー用フラグメント「header.html」の記述として適切なものを選択

layout.html

```html
<!DOCTYPE html>
<html xmlns:th="http://www.thymeleaf.org">
<body>
<div th:insert="～{common/header::header}">
</div>
  :
</body>
</html>
```

A:

```html
<th:block
th:fragment="header">
  :
</th:block>
```

## Q15

ページネーションに対応したHandlerメソッドの実装で不適切なものを選択

```java
@GetMapping("/employee_lset")
public String showList(Model model,
@PageableDefault(page=0,size=5,sort="salary") Pageable pageable){

Page<Employee> employees = repository.findAll(pageable);
  model.addAttribute("employees",employees);
  model.addAttribute("main","employee/list::main");
  return "common/layout";
}
```

リポジトリのfindAllメソッドの戻り値はEmployeeを要素としてListオブジェクトである

## Q16

コントローラに実装したHandlerメソッド社員情報を登録する際のフラッシュメッセージをセットしている
この場合のテンプレートにフラッシュメッセージを出力するための適切なもの

```java
@PostMapping("/employee_add")
public string addEmployee(@Validated
@ModelAttribute Employee employee,BindingResult result,Model model,
RedirectAttributes attrs){
    if (result.hasErrors()){
         model.addAttribute("main","employee/form::main");
         return "common/layout";
    }
    repository.save(emplyee);
    attrs.addFlashAttribute("update_success","社員")
```

A:

```html
<div th:if="${update_success}">
[[${update_success}]]</div>
```

## Q17

Spring Security設定クラスのconfigure(HttpSecurity http)の実装です。このメソッドの説明として不適切なものを選択

```java
public SecurityFilterChain
configure(HttpSecurity http) throws Exception
{
    http.csrf(csrf -> csrf.disable());
    http.authorizeHttpRequests(authorize ->{
                authorize
                .requestMatchers("/login").permitAll()
                .requestMatchers("/register").permitAll()
                .requestMatchers("/js/**").permitAll()
                .requestMatchers("/css/**").permitAll()
                .requestMatchers("/img/**").permitAll()
                .requestMatchers("/admin/**").hasRole("ADMIN")
                .anyRequest().authenticated();
     });
     http.formLogin(form ->{
             form.defaultSuccessUrl("/",true)
             .loginPage("/login")
             .loginProcessingUrl("/login");
     });
     http.logout(logout ->{
             logout.logoutUrl("/logout")
            .logoutSuccessUrl("/login?logout");
     });
     return http.build();
}
```

A:

http.csrf(csrf -> csrf.disable());の設定によりCSRF対策が有効化される

## Q18

次の例はアスペクトに実装したアドバイスの例です。
この説明として不適切なものを選択する
なお、executionのことをポイントカット指定子と呼ぶ

```java
@Before("execution(* jp.co.ctct.controller.*.*(..))")
public void beforeLog(JoinPoint joinPoint){
         :
}
```

A:

ポイントカット指定子の最初の引数「*」は全てのパッケージであることを意味する

## Q19

ユニットテストの記述として適切なものを選択

A:

通常、関数やメソッドがユニットテストの単位となる

## Q20

Spring Testにおいてテストケースに用いるアサーションメソッドとその説明として不適切なものを選択

A:
assertNull(a)
aがnullでないことを検証
